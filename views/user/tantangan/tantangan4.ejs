<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>⚔️ Want vs Need Warrior</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --putih: #ffffff;
            --kuning: #ffe44d;
            --ungu: #6559ff;
            --navy: #2D2C56ff;
            --hitam: #0f0f0f;
        }
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--ungu);
            min-height: 100vh;
            color: var(--hitam);
        }
        .maincontainer {
            background: var(--kuning);
        }
        .progress-bar {
            background: #ffffff;
            height: 12px;
            border-radius: 9999px;
            overflow: hidden;
        }
        .progress-fill {
            height: 100%;
            background: var(--kuning);
            border-radius: 9999px;
            transition: width 0.3s ease;
            width: 0%;
        }
        .stats-card {
            background: var(--putih);
            color: var(--navy);
        }
        .form-section {
            background: var(--putih);
        }
        .list-section {
            background: var(--putih);
            color: var(--navy);
        }
        .analysis-section {
            background: var(--putih);
        }
        .expense-item {
            background: var(--navy);
            color: var(--putih);
        }
        .expense-item .category {
            background: var(--kuning);
            color: var(--navy);
        }
        .expense-item .amount {
            color: var(--kuning);
        }
        .expense-item .desc {
            color: var(--putih);
        }
        .achievement {
            background: var(--kuning);
        }
        .modal-bg {
            background: rgba(0,0,0,0.5);
        }
        .modal {
            animation: slideIn 0.5s ease;
        }
        @keyframes slideIn {
            from { transform: translateY(-20px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        *:focus {
            outline: none !important;
            box-shadow: none !important;
        }
    </style>
</head>
<body>
    <div class="top-container max-w-screen-xl mx-auto px-4 md:px-20 flex items-center justify-between mb-10 mt-14">
        <div class="flex-1 text-left">
            <button id="prev-challenge" onclick="window.location.href='http://localhost:3000/tantangan/3'" class="bg-[var(--kuning)] text-[var(--navy)] font-bold py-2 px-6 rounded-full border-2 border-[var(--ungu)] transition hover:bg-[var(--navy)] hover:text-[var(--kuning)]">
                &larr; Tantangan Sebelumnya
            </button>
        </div>
        <div class="flex-1 flex justify-center px-4 middle-container text-[var(--navy)]">
            <div class="w-full text-center">
                <h1 class="text-xl md:text-2xl bg-[var(--kuning)] text-[var(--navy)] font-bold rounded-full px-8 py-3 shadow inline-block border-2">
                    Tantangan 4: Want vs Need Warrior
                </h1>
            </div>
        </div>
        <div class="flex-1 text-right">
            <button id="next-challenge" onclick="window.location.href='http://localhost:3000/tantangan/5'" class="bg-[var(--kuning)] text-[var(--navy)] font-bold py-2 px-6 rounded-full border-2 border-[var(--ungu)] transition hover:bg-[var(--navy)] hover:text-[var(--kuning)]">
                Tantangan Selanjutnya &rarr;
            </button>
        </div>
    </div>

    <div class="w-full flex justify-center">
        <div class="h-1 bg-[var(--kuning)] rounded-full mt-2 mb-8" style="width: 100%; max-width: 1090px;"></div>
    </div>

    <div class="maincontainer max-w-screen-xl mx-auto px-4 py-8 rounded-3xl shadow-lg">
        <div class="rounded-3xl shadow-lg bg-[var(--navy)] text-[var(--kuning)] px-8 py-8 mb-8 relative overflow-hidden border-2 border-[var(--kuning)]">
            <div class="absolute left-6 top-6 text-6xl opacity-20 select-none pointer-events-none">⚔️</div>
            <h2 class="text-3xl md:text-4xl font-bold mb-2 inline-block bg-[var(--kuning)] text-[var(--navy)] px-6 py-2 rounded-full shadow border-2">
                ⚔️ Want vs Need Warrior
            </h2>
            <p class="text-lg mt-4 mb-4 max-w-2xl text-center text-[var(--putih)]">
                Bersiap untuk pertempuran epik antara keinginan dan kebutuhan! Kamu harus jadi hakim yang bijak untuk setiap pengeluaran. Siapa yang menang?
            </p>
            <ul class="bg-[var(--putih)] rounded-xl px-6 py-4 mb-4 text-base mx-auto text-[var(--navy)] border border-[var(--kuning)]">
                <li><strong>📅 Tugas:</strong> Selama <b>5 hari</b>, setiap kali mau beli sesuatu, kamu harus pause dan kategorikan dulu: <b>"BUTUH BANGET"</b>, <b>"PENGEN AJA"</b>, atau <b>"GAPAPA SKIP"</b>. Minimal <b>10 keputusan pembelian</b>.</li>
                <li><strong>✅ Selesai jika:</strong> Berhasil mengkategorikan minimal <b>10 keputusan pembelian</b> dalam 5 hari, dengan minimal <b>3 keputusan "SKIP" atau "tunda beli"</b>.</li>
                <li><strong>❌ Tidak selesai jika:</strong> Kurang dari 10 keputusan yang dicatat, atau tidak ada satupun keputusan "SKIP".</li>
                <li><strong>🎖️ Hadiah:</strong> Badge <b>"Priority Prince/Princess"</b> + <b>175 poin</b></li>
            </ul>
            <div class="progress-bar mb-2">
                <div class="progress-fill" id="progressFill"></div>
            </div>
            <p id="progressText" class="text-sm text-[var(--kuning)] font-semibold">0/10 keputusan tercatat</p>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-5 gap-4 mb-8">
            <div class="stats-card rounded-2xl shadow p-6 flex flex-col items-center">
                <div class="text-3xl mb-2">📝</div>
                <div class="text-2xl font-bold" id="totalDecisions">0</div>
                <div class="text-gray-500">Total Keputusan</div>
            </div>
            <div class="stats-card rounded-2xl shadow p-6 flex flex-col items-center">
                <div class="text-3xl mb-2">⏳</div>
                <div class="text-2xl font-bold" id="daysTracked">0</div>
                <div class="text-gray-500">Hari Dicatat</div>
            </div>
            <div class="stats-card rounded-2xl shadow p-6 flex flex-col items-center">
                <div class="text-3xl mb-2">✅</div>
                <div class="text-2xl font-bold" id="needCount">0</div>
                <div class="text-gray-500">Butuh Banget</div>
            </div>
            <div class="stats-card rounded-2xl shadow p-6 flex flex-col items-center">
                <div class="text-3xl mb-2">💭</div>
                <div class="text-2xl font-bold" id="wantCount">0</div>
                <div class="text-gray-500">Pengen Aja</div>
            </div>
            <div class="stats-card rounded-2xl shadow p-6 flex flex-col items-center">
                <div class="text-3xl mb-2">🚫</div>
                <div class="text-2xl font-bold" id="skipCount">0</div>
                <div class="text-gray-500">Gapapa Skip</div>
            </div>
        </div>

        <div class="grid md:grid-cols-2 gap-8 mb-8">
            <div class="form-section rounded-2xl shadow p-8">
                <h2 class="text-xl font-bold mb-6 flex items-center gap-2 text-[var(--navy)]">🛒 Catat Keputusan Pembelian</h2>
                <form id="decisionForm">
                    <div class="mb-4">
                        <div class="bg-[#2D2C56] rounded-xl p-0 mb-4">
                            <div class="font-bold text-[var(--putih)] px-6 pt-6 pb-2 text-base">Tanggal *</div>
                            <div class="px-6 pb-6">
                                <input type="date" id="date" required
                                    class="w-full rounded-xl px-4 py-4 bg-[#2D2C56] text-[var(--putih)] placeholder:text-gray-300 border-0 focus:ring-2 focus:ring-[var(--kuning)] transition" />
                            </div>
                        </div>
                    </div>
                    <div class="mb-4">
                        <div class="bg-[#2D2C56] rounded-xl p-0 mb-4">
                            <div class="font-bold text-[var(--putih)] px-6 pt-6 pb-2 text-base">Barang/Jasa *</div>
                            <div class="px-6 pb-6">
                                <input type="text" id="item" placeholder="Contoh: Boba, Pulsa, Jajan, dll" required
                                    class="w-full rounded-xl px-4 py-4 bg-[#2D2C56] text-[var(--putih)] placeholder:text-gray-300 border-0 focus:ring-2 focus:ring-[var(--kuning)] transition" />
                            </div>
                        </div>
                    </div>
                    <div class="mb-4">
                        <div class="bg-[#2D2C56] rounded-xl p-0 mb-4">
                            <div class="font-bold text-[var(--putih)] px-6 pt-6 pb-2 text-base">Keputusan *</div>
                            <div class="px-6 pb-6">
                                <select id="decision" required
                                    class="w-full rounded-xl px-4 py-4 bg-[#2D2C56] text-[var(--putih)] border-0 focus:ring-2 focus:ring-[var(--kuning)] transition">
                                    <option value="">Pilih keputusan...</option>
                                    <option value="BUTUH BANGET">BUTUH BANGET</option>
                                    <option value="PENGEN AJA">PENGEN AJA</option>
                                    <option value="GAPAPA SKIP">GAPAPA SKIP</option>
                                </select>
                            </div>
                        </div>
                    </div>
                    <div class="mb-6">
                        <div class="bg-[#2D2C56] rounded-xl p-0 mb-4">
                            <div class="font-bold text-[var(--putih)] px-6 pt-6 pb-2 text-base">Alasan/Keterangan *</div>
                            <div class="px-6 pb-6">
                                <textarea id="reason" rows="2" placeholder="Kenapa kamu memilih keputusan ini?" required
                                    class="w-full rounded-xl px-4 py-4 bg-[#2D2C56] text-[var(--putih)] placeholder:text-gray-300 border-0 focus:ring-2 focus:ring-[var(--kuning)] transition"></textarea>
                            </div>
                        </div>
                    </div>
                    <button type="submit"
                        class="w-full bg-[var(--kuning)] text-[var(--navy)] font-bold py-3 rounded-xl shadow hover:bg-yellow-300 transition border-0">➕ Tambah Keputusan</button>
                </form>

                <button id="finishBtn" class="w-full mt-4 bg-[var(--ungu)] text-white font-bold py-3 rounded-xl shadow hover:bg-[var(--navy)] transition hidden">
                    Klaim Hadiah & Selesaikan
                </button>
            </div>
            
            <div class="list-section rounded-2xl shadow p-8">
                <h2 class="text-xl font-bold mb-6 text-[var(--navy)]">📋 Daftar Keputusan</h2>
                <div id="decisionsList">
                    <p class="text-center text-gray-400 mt-12">
                        Belum ada keputusan tercatat.<br>
                        Mulai catat setiap keputusan pembelianmu! ⚔️
                    </p>
                </div>
            </div>
        </div>

        <div class="analysis-section rounded-2xl shadow p-8 mb-8">
            <h2 class="text-xl font-bold mb-4 text-[var(--navy)]">📊 Analisis Keputusan</h2>
            <div id="analysisContent">
                <p class="text-center text-gray-400">
                    Catat minimal 10 keputusan untuk melihat analisis lengkap! 🛡️
                </p>
            </div>
        </div>

        <div class="achievement text-[var(--navy)] font-bold p-8 rounded-2xl text-center mt-6 shadow" id="achievement" style="display:none;">
            <div id="achievementText"></div>
        </div>

        <div id="badgeModal" class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-40" style="display:none;">
            <div id="badgeCard" class="bg-[var(--putih)] rounded-3xl shadow-2xl p-8 max-w-xs w-full flex flex-col items-center relative scale-90 opacity-0 transition-all duration-500 modal">
                <button onclick="closeBadgeModal()" class="absolute top-3 right-4 text-2xl text-[var(--navy)] hover:text-red-500 font-bold">&times;</button>
                <img src="https://github.com/luthfibphoenix/apmul/blob/main/B4M4.png?raw=true" alt="Badge" class="w-24 h-24 mb-4" />
                <h3 class="text-xl font-bold text-[var(--navy)] mb-2">Badge Unlocked!</h3>
                <div class="bg-[var(--kuning)] text-[var(--navy)] font-bold rounded-full px-6 py-2 mb-2 shadow border-2 border-[var(--ungu)]">
                    Priority Prince/Princess
                </div>
                <div class="text-lg text-[var(--navy)] mb-2">+175 Poin</div>
                <p class="text-sm text-gray-500">Kamu berhasil menaklukkan keinginan dan jadi pejuang prioritas sejati! ⚔️</p>
            </div>
        </div>
    </div>
    <script>
        let decisions = [];

        // Set tanggal hari ini sebagai default
        document.getElementById('date').valueAsDate = new Date();

        document.getElementById('decisionForm').addEventListener('submit', function(e) {
            e.preventDefault();

            const date = document.getElementById('date').value;
            const item = document.getElementById('item').value.trim();
            const decision = document.getElementById('decision').value;
            const reason = document.getElementById('reason').value.trim();

            if (!date || !item || !decision || !reason) {
                alert('Semua field wajib diisi! 📝');
                return;
            }

            decisions.push({
                id: Date.now(),
                date,
                item,
                decision,
                reason
            });

            updateDisplay();
            this.reset();
            document.getElementById('date').valueAsDate = new Date();
        });

        function updateDisplay() {
            updateStats();
            updateDecisionsList();
            updateProgress();
            updateAnalysis();
            checkAchievement();
        }

        function updateStats() {
            const totalDecisions = decisions.length;
            const uniqueDates = [...new Set(decisions.map(d => d.date))].length;
            const needCount = decisions.filter(d => d.decision === "BUTUH BANGET").length;
            const wantCount = decisions.filter(d => d.decision === "PENGEN AJA").length;
            const skipCount = decisions.filter(d => d.decision === "GAPAPA SKIP").length;

            document.getElementById('totalDecisions').textContent = totalDecisions;
            document.getElementById('daysTracked').textContent = uniqueDates;
            document.getElementById('needCount').textContent = needCount;
            document.getElementById('wantCount').textContent = wantCount;
            document.getElementById('skipCount').textContent = skipCount;
        }

        function updateDecisionsList() {
            const decisionsList = document.getElementById('decisionsList');
            if (decisions.length === 0) {
                decisionsList.innerHTML = `
                    <p class="text-center text-gray-400 mt-12">
                        Belum ada keputusan tercatat.<br>
                        Mulai catat setiap keputusan pembelianmu! ⚔️
                    </p>
                `;
                return;
            }

            const sortedDecisions = [...decisions].sort((a, b) => new Date(b.date) - new Date(a.date) || b.id - a.id);
            decisionsList.innerHTML = sortedDecisions.map(d => `
                <div class="expense-item rounded-xl p-4 mb-3 flex justify-between items-start">
                    <div class="flex-1 mr-4">
                        <div class="flex flex-wrap items-center justify-between mb-2 gap-2">
                             <span class="category text-xs font-bold rounded-full px-3 py-1">${d.decision}</span>
                             <span class="amount text-lg font-bold text-right">${d.item}</span>
                        </div>
                        <div class="desc text-sm text-gray-300">
                            <span>📅 ${new Date(d.date).toLocaleDateString('id-ID', { day: '2-digit', month: 'long', year: 'numeric'})}</span><br>
                            <span class="italic">"${d.reason}"</span>
                        </div>
                    </div>
                    <button class="delete-btn bg-red-600 text-white rounded-full h-8 w-8 flex items-center justify-center font-bold text-xs flex-shrink-0 hover:bg-red-700 transition" onclick="deleteDecision(${d.id})">🗑️</button>
                </div>
            `).join('');
        }
        
        function deleteDecision(id) {
            decisions = decisions.filter(d => d.id !== id);
            updateDisplay();
        }

        function updateProgress() {
            const progress = Math.min((decisions.length / 10) * 100, 100);
            document.getElementById('progressFill').style.width = `${progress}%`;
            document.getElementById('progressText').textContent = `${decisions.length}/10 keputusan tercatat`;
        }

        function updateAnalysis() {
            const analysisContent = document.getElementById('analysisContent');
            if (decisions.length < 10) {
                analysisContent.innerHTML = `
                    <p class="text-center text-gray-400">
                        Catat minimal 10 keputusan untuk melihat analisis lengkap! 🛡️<br>
                        <small>Progress: ${decisions.length}/10 keputusan</small>
                    </p>
                `;
                return;
            }

            const needCount = decisions.filter(d => d.decision === "BUTUH BANGET").length;
            const wantCount = decisions.filter(d => d.decision === "PENGEN AJA").length;
            const skipCount = decisions.filter(d => d.decision === "GAPAPA SKIP").length;

            analysisContent.innerHTML = `
                <h3 class="font-bold text-lg mb-2 text-center">⚔️ Hasil Pertempuran Keputusanmu ⚔️</h3>
                <div class="flex justify-around text-center mt-4">
                    <div><div class="text-3xl">✅</div><div class="font-bold text-2xl">${needCount}</div><div>Butuh</div></div>
                    <div><div class="text-3xl">💭</div><div class="font-bold text-2xl">${wantCount}</div><div>Pengen</div></div>
                    <div><div class="text-3xl">🚫</div><div class="font-bold text-2xl">${skipCount}</div><div>Skip</div></div>
                </div>
                <div class="bg-yellow-100 p-4 rounded-lg mt-6 border-l-4 border-yellow-500 text-yellow-800">
                    <strong>Tips Pejuang:</strong> Setiap keputusan "SKIP" adalah kemenangan! Itu adalah uang yang bisa kamu gunakan untuk tujuan yang lebih besar. Kamu hebat!
                </div>
            `;
        }

        function checkAchievement() {
            const totalDecisions = decisions.length;
            const uniqueDates = [...new Set(decisions.map(d => d.date))].length;
            const skipCount = decisions.filter(d => d.decision === "GAPAPA SKIP").length;
            
            const achievementDiv = document.getElementById('achievement');
            const achievementText = document.getElementById('achievementText');
            const finishBtn = document.getElementById('finishBtn');
            
            // Inilah kondisi untuk menyelesaikan tantangan
            const isComplete = totalDecisions >= 10 && uniqueDates >= 5 && skipCount >= 3;
            
            if (isComplete) {
                achievementText.innerHTML = `
                    <h3 class="text-2xl font-bold mb-2">🎉 Selamat, Tantangan Selesai! 🎉</h3>
                    <p>Kamu berhasil mencatat <b>${totalDecisions}</b> keputusan dalam <b>${uniqueDates}</b> hari, dan menahan diri sebanyak <b>${skipCount}</b> kali. Hebat!</p>
                `;
                achievementDiv.style.display = 'block'; // Menampilkan kotak teks selamat
                finishBtn.classList.remove('hidden');   // Menampilkan tombol selesai
            } else {
                // Jika kondisi tidak terpenuhi, sembunyikan kembali
                achievementDiv.style.display = 'none';
                finishBtn.classList.add('hidden');
            }
        }

        // Modal logic
        function showBadgeModal() {
            const modal = document.getElementById('badgeModal');
            const card = document.getElementById('badgeCard');
            modal.style.display = 'flex';
            setTimeout(() => {
                card.style.transform = 'scale(1)';
                card.style.opacity = '1';
            }, 50);
        }

        function closeBadgeModal() {
            const card = document.getElementById('badgeCard');
            card.style.transform = 'scale(0.9)';
            card.style.opacity = '0';
            setTimeout(() => {
                document.getElementById('badgeModal').style.display = 'none';
            }, 400);
        }
        
        // Gabungkan event listener untuk tombol selesai menjadi satu fungsi
        async function handleFinish() {
             // Tampilkan modal hadiah terlebih dahulu untuk pengalaman pengguna yang lancar
            showBadgeModal();
            
            // Coba kirim data ke server di latar belakang
            try {
                const res = await fetch('/tantangan/4/finish', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    // Jika perlu mengirim data, tambahkan body: JSON.stringify({ decisions })
                });

                if (res.ok) {
                    const data = await res.json();
                    if (data.success) {
                        console.log("Progres berhasil disimpan di server!");
                    } else {
                        console.error("Server merespon, tapi progres gagal disimpan.", data.message);
                    }
                } else {
                    // Ini akan terjadi jika server mengembalikan error seperti 404 atau 500
                    console.error("Gagal menghubungi server. Status:", res.status);
                }
            } catch (err) {
                // Ini akan terjadi jika ada masalah jaringan atau file dibuka secara lokal
                console.warn("Terjadi kesalahan koneksi. Tidak dapat menyimpan progres ke server. Error:", err.message);
            }
        }

        // Pasang event listener ke tombol "Selesai"
        document.getElementById('finishBtn').addEventListener('click', handleFinish);

        // Inisialisasi tampilan saat halaman dimuat
        updateDisplay();
    </script>
</body>
</html>