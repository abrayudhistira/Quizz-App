<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preview - Penutup Modul 2</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --accent-color: #e74c3c;
            --success-color: #27ae60;
            --warning-color: #f39c12;
            --light-bg: #ecf0f1;
            --dark-text: #2c3e50;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            color: var(--dark-text);
        }

        .header-section {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: rgb(167, 170, 2);
            padding: 4rem 0;
            position: relative;
            overflow: hidden;
        }

        .header-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="white" opacity="0.1"/><circle cx="75" cy="75" r="1" fill="white" opacity="0.1"/><circle cx="50" cy="10" r="0.5" fill="white" opacity="0.1"/><circle cx="90" cy="40" r="0.5" fill="white" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            pointer-events: none;
        }

        .conclusion-card {
            background: rgb(255, 255, 255);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 3rem;
            margin: 2rem 0;
            position: relative;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .conclusion-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 25px 50px rgba(0,0,0,0.15);
        }

        .conclusion-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, var(--secondary-color), var(--success-color), var(--warning-color));
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin: 3rem 0;
        }

        .summary-item {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .summary-item:hover {
            transform: translateY(-5px);
            border-color: var(--secondary-color);
            box-shadow: 0 15px 40px rgba(0,0,0,0.15);
        }

        .summary-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 1.5rem;
            background: linear-gradient(135deg, var(--secondary-color), var(--success-color));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 2rem;
        }

        .cta-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 25px;
            padding: 4rem;
            margin: 4rem 0;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .cta-section::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            animation: pulse 4s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.5; }
            50% { transform: scale(1.1); opacity: 0.8; }
        }

        .action-buttons {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .action-btn {
            background: white;
            color: var(--primary-color);
            border: none;
            border-radius: 15px;
            padding: 1.5rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            transition: all 0.3s ease;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }

        .action-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
            color: var(--primary-color);
        }

        .action-btn.quiz {
            background: linear-gradient(135deg, var(--warning-color), #e67e22);
            color: white;
        }

        .action-btn.challenge {
            background: linear-gradient(135deg, var(--accent-color), #c0392b);
            color: white;
        }

        .action-btn.next {
            background: linear-gradient(135deg, var(--success-color), #2ecc71);
            color: white;
        }

        .progress-indicator {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            margin: 3rem 0;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .progress-bar-custom {
            height: 20px;
            border-radius: 10px;
            background: var(--light-bg);
            overflow: hidden;
            margin: 1rem 0;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--secondary-color), var(--success-color));
            border-radius: 10px;
            width: 100%;
            position: relative;
            animation: fillProgress 2s ease-out;
        }

        @keyframes fillProgress {
            from { width: 0%; }
            to { width: 100%; }
        }

        .celebration-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1000;
        }

        .confetti {
            position: absolute;
            width: 10px;
            height: 10px;
            background: var(--warning-color);
            animation: confetti-fall 3s linear infinite;
        }

        @keyframes confetti-fall {
            to {
                transform: translateY(100vh) rotate(360deg);
            }
        }

        .motivational-quote {
            background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
            border-radius: 20px;
            padding: 3rem;
            margin: 3rem 0;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .quote-icon {
            font-size: 4rem;
            color: rgba(0,0,0,0.1);
            position: absolute;
            top: 1rem;
            left: 2rem;
        }

        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease-out forwards;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .stats-counter {
            display: flex;
            justify-content: space-around;
            margin: 2rem 0;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 3rem;
            font-weight: bold;
            color: var(--secondary-color);
            display: block;
        }

        .stat-label {
            font-size: 0.9rem;
            color: #7f8c8d;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        @media (max-width: 768px) {
            .header-section {
                padding: 2rem 0;
            }
            
            .conclusion-card {
                padding: 2rem;
                margin: 1rem 0;
            }
            
            .cta-section {
                padding: 2rem;
                margin: 2rem 0;
            }
            
            .summary-grid {
                grid-template-columns: 1fr;
                gap: 1rem;
            }
            
            .action-buttons {
                grid-template-columns: 1fr;
                gap: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="celebration-animation" id="celebration"></div>

    <!-- Header Section -->
    <div class="header-section">
        <div class="container">
            <div class="row justify-content-center text-center">
                <div class="col-lg-8 fade-in">
                    <i class="fas fa-trophy mb-4" style="font-size: 4rem;"></i>
                    <h1 class="display-4 fw-bold mb-3">Selamat, Kamu Sudah Sampai di Akhir Modul 2!</h1>
                    <p class="lead mb-4">Modul 2: Tabungan itu Gaya Hidup Bukan Paksaan</p>
                    <div class="progress-indicator">
                        <h5 class="mb-3">Progress Pembelajaran</h5>
                        <div class="progress-bar-custom">
                            <div class="progress-fill"></div>
                        </div>
                        <p class="mt-2 mb-0"><strong>Modul 2 - Selesai 100%</strong></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container my-5">
        <!-- Kesimpulan Section -->
        <div class="conclusion-card fade-in">
            <div class="text-center mb-4">
                <i class="fas fa-lightbulb text-warning" style="font-size: 3rem;"></i>
                <h2 class="mt-3 mb-4">Kesimpulan Modul 2</h2>
            </div>
            <div class="row">
                <div class="col-lg-10 mx-auto">
                    <ul style="font-size:1.1rem;line-height:1.7;">
                        
                        <li>
                            <strong>Section 2 – Mitos vs Fakta:</strong>
                            Melalui fitur interaktif, kamu bisa membedakan mana mitos dan fakta soal menabung. Ini membantu membangun pemahaman yang benar dan menghilangkan anggapan keliru tentang tabungan.
                        </li>
                        <li>
                            <strong>Section 3 – Micro-Saving:</strong>
                            Dengan simulator dan infografik, kamu belajar bahwa menabung tidak harus langsung besar. Mulai dari nominal kecil setiap hari, progress-mu tetap nyata dan bisa dipantau dengan mudah.
                        </li>
                        <li>
                            <strong>Section 4 – Tentukan Tujuan Nabung:</strong>
                            Kamu diajak menentukan target tabungan pribadi dengan tools visual yang inspiratif. Menabung jadi lebih terarah dan terasa menyenangkan karena ada tujuan yang jelas.
                        </li>
                        <li>
                            <strong>Section 5 – Penutup Modul 2:</strong>
                            Modul ini menegaskan bahwa menabung adalah gaya hidup, bukan paksaan. Kamu siap melanjutkan ke quiz dan tantangan untuk membuktikan pemahamanmu!
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Summary Grid -->
        <div class="summary-grid">
            <div class="summary-item fade-in">
                <div class="summary-icon">
                    <i class="fas fa-brain"></i>
                </div>
                <h4>Pola Pikir Baru</h4>
                <p>Kamu sudah membangun mindset bahwa menabung itu bukan paksaan, tapi gaya hidup yang bisa bikin hidup lebih tenang.</p>
            </div>
            <div class="summary-item fade-in">
                <div class="summary-icon">
                    <i class="fas fa-calendar-check"></i>
                </div>
                <h4>Kebiasaan Positif</h4>
                <p>Menabung secara rutin, sekecil apapun, akan membentuk kebiasaan baik dan membawa dampak besar di masa depan.</p>
            </div>
            <div class="summary-item fade-in">
                <div class="summary-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h4>Dukungan Komunitas</h4>
                <p>Menabung bareng teman atau komunitas membuat prosesnya lebih seru dan memotivasi untuk tetap konsisten.</p>
            </div>
            <div class="summary-item fade-in">
                <div class="summary-icon">
                    <i class="fas fa-smile-beam"></i>
                </div>
                <h4>Menabung = Bahagia</h4>
                <p>Riset membuktikan, menabung secara rutin meningkatkan rasa aman dan kebahagiaan, bahkan dari nominal kecil.</p>
            </div>
        </div>

        <!-- Motivational Quote -->
        <div class="motivational-quote fade-in">
            <i class="fas fa-quote-left quote-icon"></i>
            <h3 class="mb-3" style="position: relative; z-index: 1;">"Kebiasaan kecil hari ini, bisa jadi kebebasan besar di masa depan."</h3>
            <p class="mb-0" style="position: relative; z-index: 1;"><em>Mulai dari sekarang, jadikan menabung sebagai gaya hidupmu!</em></p>
        </div>

        <!-- CTA Section -->
        <div class="cta-section fade-in">
            <h2 class="mb-4">Yuk, Uji Pemahamanmu!</h2>
            <p class="lead mb-4">
                Jangan berhenti di sini. Buktikan pemahamanmu dengan mengerjakan quiz dan tantangan yang sudah disiapkan. Siap untuk langkah berikutnya?
            </p>
            <div class="action-buttons">
                <a href="#" class="action-btn quiz" onclick="alert('Quiz akan dimulai!')">
                    <i class="fas fa-question-circle"></i>
                    <span>
                        <strong>Kerjakan Quiz</strong><br>
                        <small>Uji pemahaman materi</small>
                    </span>
                </a>
                <a href="#" class="action-btn challenge" onclick="alert('Tantangan siap diambil!')">
                    <i class="fas fa-fire"></i>
                    <span>
                        <strong>Ambil Tantangan</strong><br>
                        <small>Praktikkan kebiasaan menabung</small>
                    </span>
                </a>
                <a href="#" class="action-btn next" onclick="alert('Lanjut ke Modul 3!')">
                    <i class="fas fa-arrow-right"></i>
                    <span>
                        <strong>Lanjut ke Modul 3</strong><br>
                        <small>Tingkatkan level finansialmu</small>
                    </span>
                </a>
            </div>
        </div>

        <!-- Achievement Badge -->
        <div class="conclusion-card text-center fade-in">
            <div class="mb-4">
                <i class="fas fa-medal text-warning" style="font-size: 5rem;"></i>
            </div>
            <h3 class="mb-3">Pencapaianmu</h3>
            <div class="row">
                <div class="col-md-4">
                    <div class="border rounded p-3 mb-3">
                        <i class="fas fa-check-circle text-success fs-2"></i>
                        <h5 class="mt-2">Mindset Berubah</h5>
                        <p class="small text-muted">Kamu sudah punya pola pikir baru tentang menabung</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="border rounded p-3 mb-3">
                        <i class="fas fa-star text-warning fs-2"></i>
                        <h5 class="mt-2">Kebiasaan Terbentuk</h5>
                        <p class="small text-muted">Sudah mulai menabung secara rutin</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="border rounded p-3 mb-3">
                        <i class="fas fa-rocket text-info fs-2"></i>
                        <h5 class="mt-2">Siap Level Up</h5>
                        <p class="small text-muted">Siap lanjut ke modul berikutnya</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Final Message -->
        <div class="text-center mt-5 fade-in">
            <h4 class="mb-3">Terima kasih sudah belajar dengan semangat!</h4>
            <p class="text-muted">
                Perjalananmu baru dimulai. Terus praktikkan kebiasaan baik ini, dan jadikan menabung sebagai bagian dari gaya hidupmu.
            </p>
            <div class="mt-4">
                <a href="#" class="btn btn-outline-primary btn-lg me-3" onclick="alert('Kembali ke Dashboard')">
                    <i class="fas fa-home me-2"></i>Kembali ke Dashboard
                </a>
                <a href="#" class="btn btn-outline-secondary btn-lg" onclick="alert('Lihat Progress Anda')">
                    <i class="fas fa-chart-bar me-2"></i>Lihat Progress
                </a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Celebration animation
        function createConfetti() {
            const celebration = document.getElementById('celebration');
            const colors = ['#e74c3c', '#f39c12', '#2ecc71', '#3498db', '#9b59b6'];
            
            for (let i = 0; i < 30; i++) {
                setTimeout(() => {
                    const confetti = document.createElement('div');
                    confetti.className = 'confetti';
                    confetti.style.left = Math.random() * 100 + '%';
                    confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                    confetti.style.animationDelay = Math.random() * 3 + 's';
                    confetti.style.animationDuration = (Math.random() * 3 + 2) + 's';
                    celebration.appendChild(confetti);
                    
                    setTimeout(() => {
                        confetti.remove();
                    }, 5000);
                }, i * 100);
            }
        }

        // Counter animation
        function animateCounters() {
            const counters = document.querySelectorAll('[data-count]');
            
            counters.forEach(counter => {
                const target = parseInt(counter.getAttribute('data-count'));
                const duration = 2000;
                const step = target / (duration / 16);
                let current = 0;
                
                const timer = setInterval(() => {
                    current += step;
                    if (current >= target) {
                        current = target;
                        clearInterval(timer);
                    }
                    counter.textContent = Math.floor(current);
                }, 16);
            });
        }

        // Initialize animations
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(createConfetti, 500);
            
            // Add hover effects to action buttons
            document.querySelectorAll('.action-btn').forEach(btn => {
                btn.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-5px) scale(1.02)';
                });
                
                btn.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(-3px) scale(1)';
                });
            });
        });
    </script>
</body>
</html>