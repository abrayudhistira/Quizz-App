<section id="m3s2" class="w-full py-10 px-4 md:px-10 rounded-2xl">
  <div class="bg-[var(--putih)] rounded-3xl px-6 py-10 max-w-screen-xl mx-auto">

    <div class="text-center mb-10">
      <h2 class="text-2xl md:text-4xl font-bold mb-4">Apa Itu Budgeting?</h2>
      <p class="text-base md:text-lg text-gray-600">
        Budgeting itu strategi mengatur uang biar nggak cepat habis, kayak main game: ada point, ada misi, dan harus atur strategi supaya menang!
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-10 mb-10">
      
      <div class="w-full aspect-square rounded-xl overflow-hidden shadow">
        <img src="https://github.com/abdullahmaajid/imagefundzy/blob/main/m3s2.png?raw=true" alt="Ilustrasi Karakter Budgeting" class="w-full h-full object-cover" />
      </div>
      
      <div class="w-full aspect-square bg-gray-50 rounded-xl p-6 shadow flex flex-col justify-center space-y-4">
        <div x-data="{ open: false }" class="border rounded-lg overflow-hidden shadow-sm bg-white">
          <button @click="open = !open" class="w-full text-left px-6 py-4 hover:bg-gray-100 font-semibold flex justify-between items-center transition-colors">
            <span>Mitos: Budgeting itu kaku & nggak seru</span>
            <svg :class="{'rotate-180': open}" class="h-5 w-5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div x-show="open" x-transition class="px-6 py-4 text-left border-t text-sm">
            <strong>Fakta:</strong> Budgeting justru bikin kamu bebas atur uang sesuai gaya hidup, jadi lebih seru dan nggak stres!
          </div>
        </div>
        <div x-data="{ open: false }" class="border rounded-lg overflow-hidden shadow-sm bg-white">
          <button @click="open = !open" class="w-full text-left px-6 py-4 hover:bg-gray-100 font-semibold flex justify-between items-center transition-colors">
            <span>Mitos: Budgeting cuma buat orang dewasa</span>
            <svg :class="{'rotate-180': open}" class="h-5 w-5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div x-show="open" x-transition class="px-6 py-4 text-left border-t text-sm">
            <strong>Fakta:</strong> Semakin muda mulai budgeting, makin jago atur uang dan siap capai target finansial!
          </div>
        </div>
      </div>
    </div>

    <div class="w-full bg-gray-50 rounded-xl p-6 shadow text-left">
      <h3 class="font-bold text-lg mb-4 text-center">Mini-Game: Budgeting Point System</h3>
      <form id="budgetGameForm" onsubmit="event.preventDefault(); playBudgetGame();">
        <div class="mb-4">
          <label for="gameBudget" class="block font-semibold mb-2">Total Point (Budget) Kamu</label>
          <input type="number" id="gameBudget" class="w-full border rounded px-3 py-2" min="1" value="100" required>
        </div>
        <div class="mb-4">
          <label for="gameSpend" class="block font-semibold mb-2">Point yang Dipakai (Pengeluaran)</label>
          <input type="number" id="gameSpend" class="w-full border rounded px-3 py-2" min="0" value="30" required>
        </div>
        <button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 rounded hover:bg-blue-700 transition">Cek Sisa Point</button>
      </form>
      <div id="budgetGameResult" class="mt-6 hidden">
        <div class="mb-2 font-semibold">Sisa Point:</div>
        <div id="budgetGamePoint" class="text-2xl font-bold text-green-600 mb-4"></div>
        <div class="mb-2 font-semibold">Progress:</div>
        <div class="w-full bg-gray-200 rounded-full h-4">
          <div id="budgetGameBar" class="bg-green-500 h-4 rounded-full transition-all" style="width:0%"></div>
        </div>
        <div class="text-sm mt-2" id="budgetGameText"></div>
      </div>
    </div>

  </div>

  <script>
    function playBudgetGame() {
      const budget = parseInt(document.getElementById('gameBudget').value) || 0;
      const spend = parseInt(document.getElementById('gameSpend').value) || 0;
      const remain = Math.max(budget - spend, 0);
      document.getElementById('budgetGamePoint').textContent = remain + ' Point';
      document.getElementById('budgetGameResult').classList.remove('hidden');
      const percent = Math.min((remain / budget) * 100, 100);
      document.getElementById('budgetGameBar').style.width = percent + '%';
      document.getElementById('budgetGameText').textContent = 'Sisa point: ' + remain + ' dari ' + budget;
    }
  </script>
</section>