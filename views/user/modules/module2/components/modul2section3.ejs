<section id="m2s3" class="w-full py-10 px-4 md:px-10 rounded-2xl">
 <div class="bg-[var(--putih)] rounded-3xl px-6 py-10 max-w-screen-xl mx-auto space-y-10">

   <div class="text-center">
     <h2 class="text-2xl md:text-4xl font-bold mb-2">💰 Micro-Saving: Simulasi Tabungan Harian</h2>
     <p class="text-base md:text-lg text-gray-600">Yuk coba hitung berapa banyak uang yang bisa kamu kumpulkan dari kebiasaan kecil setiap hari!</p>
   </div>

   <div class="grid grid-cols-1 md:grid-cols-2 gap-10">

     <div class="bg-gray-50 rounded-xl p-6 shadow text-left">
       <form id="microSavingForm" onsubmit="event.preventDefault(); calculateSaving();">
         <div class="mb-4">
           <label for="dailyAmount" class="block font-semibold mb-1">Nominal tabungan harian (Rp)</label>
           <input type="number" id="dailyAmount" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" min="0" value="5000" required>
         </div>
         <div class="mb-4">
           <label for="days" class="block font-semibold mb-1">Jumlah hari menabung</label>
           <input type="number" id="days" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" min="1" max="31" value="30" required>
         </div>
         <button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 rounded-lg hover:bg-blue-700 transition-all duration-200">Hitung Tabungan</button>
       </form>

       <div id="resultSection" class="mt-6 hidden">
         <div class="mb-2 text-sm font-semibold">Total Tabungan:</div>
         <div id="totalSaving" class="text-2xl font-bold text-green-600 mb-4"></div>

         <div class="mb-2 text-sm font-semibold">Progress Menabung:</div>
         <div class="w-full bg-gray-200 rounded-full h-4 overflow-hidden">
           <div id="progressBar" class="bg-green-500 h-full transition-all duration-300" style="width:0%"></div>
         </div>
         <div class="text-sm mt-2 text-gray-600" id="progressText"></div>
       </div>
     </div>

     <div>
<img src="https://github.com/abdullahmaajid/imagefundzy/blob/main/m2s3.png?raw=true" alt="Ilustrasi untuk bagian m2s3" class="rounded-xl">
     </div>
   </div>
 </div>

 <script>
   function calculateSaving() {
     const daily = parseInt(document.getElementById('dailyAmount').value) || 0;
     const days = parseInt(document.getElementById('days').value) || 0;
     const maxDays = 31;

     const total = daily * days;
     const percent = Math.min((days / maxDays) * 100, 100);

     document.getElementById('totalSaving').textContent = 'Rp ' + total.toLocaleString('id-ID');
     document.getElementById('progressBar').style.width = percent + '%';
     document.getElementById('progressText').textContent = `${days} hari dari 31 hari`;

     document.getElementById('resultSection').classList.remove('hidden');
   }
 </script>
</section>