<section id="m4s2" class="w-full py-10 px-4 md:px-10 rounded-2xl">
  <div class="bg-[var(--putih)] rounded-3xl px-6 py-10 max-w-screen-xl mx-auto">
    <!-- Judul -->
    <div class="text-center mb-10">
      <h2 class="text-2xl md:text-3xl font-bold mb-4">Contoh Kasus & Decision Maker</h2>
      <p class="text-base md:text-lg text-gray-600">
        Yuk, tentukan mana yang mau dan mana yang butuh lewat contoh kasus berikut. Gunakan decision maker interaktif dan lihat konflik internal antara otak kanan dan kiri!
      </p>
    </div>

    <!-- Grid 2 Kolom -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-start">
      <!-- Kolom Kiri: Contoh Kasus -->
      <div>
        <div class="bg-blue-50 rounded-xl p-6 text-left shadow-md">
          <h3 class="font-semibold mb-2 text-blue-700">Contoh Kasus:</h3>
          <p class="text-gray-700">
            Kamu ingin membeli smartphone baru padahal smartphone lama masih berfungsi dengan baik. Apakah ini kebutuhan atau keinginan?
          </p>
        </div>
      </div>

      <!-- Kolom Kanan: Decision Maker -->
      <div>
        <div class="bg-white rounded-xl shadow p-6">
          <p class="mb-4 font-semibold">Menurut kamu, ini termasuk?</p>
          <button id="btn-keinginan" class="bg-blue-600 text-white px-4 py-2 rounded-lg mr-2 hover:bg-blue-700">Keinginan</button>
          <button id="btn-kebutuhan" class="bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700">Kebutuhan</button>
          <div id="decision-result" class="mt-4 text-base font-semibold"></div>
        </div>
      </div>
    </div>

    <!-- Bar Konflik Internal (Full Width Row) -->
    <div class="mt-8">
      <div class="bg-gray-100 rounded-xl p-6 shadow text-center">
        <p class="font-semibold mb-4">Konflik Internal: Otak Kanan vs Otak Kiri</p>
        <div class="flex items-center justify-between text-sm font-medium text-gray-700 mb-2">
          <span>🧠 Otak Kiri (Logika)</span>
          <span>🧠 Otak Kanan (Emosi)</span>
        </div>
        <div class="relative w-full h-6 bg-gray-300 rounded-full overflow-hidden">
          <div id="bar-logika" class="absolute left-0 top-0 h-full bg-green-500 transition-all duration-500" style="width: 50%"></div>
          <div id="bar-emosi" class="absolute right-0 top-0 h-full bg-blue-500 transition-all duration-500" style="width: 50%"></div>
        </div>
        <p class="text-xs text-gray-500 mt-3">Bar ini menunjukkan dominasi logika atau emosi dalam pengambilan keputusanmu.</p>
      </div>
    </div>
  </div>

  <!-- Script Interaktif -->
  <script>
    const result = document.getElementById('decision-result');
    const barLogika = document.getElementById('bar-logika');
    const barEmosi = document.getElementById('bar-emosi');

    document.getElementById('btn-keinginan').onclick = () => {
      result.innerHTML = '<span class="text-blue-700">Benar! Ini termasuk <b>keinginan</b> karena smartphone lama masih berfungsi.</span>';
      barLogika.style.width = '30%';
      barEmosi.style.width = '70%';
    };

    document.getElementById('btn-kebutuhan').onclick = () => {
      result.innerHTML = '<span class="text-red-600">Kurang tepat. Ini lebih ke <b>keinginan</b>, bukan kebutuhan utama.</span>';
      barLogika.style.width = '40%';
      barEmosi.style.width = '60%';
    };
  </script>
</section>
