<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profil</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background: #6559ff;
      min-height: 100vh;
      font-family: 'Inter', Arial, sans-serif;
    }
    .edit-profile-card {
      max-width: 900px;
      margin: 48px auto;
      border-radius: 40px;
      overflow: hidden;
      box-shadow: 0 8px 32px 0 rgba(44,44,90,0.12);
      background: transparent;
    }
    .edit-profile-header {
      position: relative;
      height: 192px;
      background: url('https://raw.githubusercontent.com/luthfibphoenix/apmul/main/Tabungan.png') center/cover no-repeat;
      border-top-left-radius: 40px;
      border-top-right-radius: 40px;
    }
    .edit-profile-back {
      position: absolute;
      top: 24px;
      left: 24px;
      background: #fff;
      color: #2D2C56;
      padding: 6px 22px;
      border-radius: 9999px;
      font-weight: 600;
      font-size: 15px;
      box-shadow: 0 2px 8px 0 rgba(44,44,90,0.08);
      text-decoration: none;
      transition: background 0.2s;
      z-index: 30;
      border: none;
      outline: none;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .edit-profile-back:hover {
      background: #f3f3f3;
    }
    .edit-profile-avatar-wrap {
      position: absolute;
      left: 48px;
      bottom: -64px;
      z-index: 20;
      background: #fff;
      border-radius: 9999px;
      border: 4px solid #FFE149;
      box-shadow: 0 4px 24px 0 rgba(44,44,90,0.08);
      width: 128px;
      height: 128px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .edit-profile-avatar-img {
      width: 112px;
      height: 112px;
      object-fit: contain;
      border-radius: 9999px;
      background: #fff;
    }
    .edit-profile-form-section {
      background: #FFE149;
      border-bottom-left-radius: 40px;
      border-bottom-right-radius: 40px;
      padding: 96px 48px 48px 48px;
      min-height: 400px;
      position: relative;
    }
    .edit-profile-form-section h2 {
      color: #2D2C56;
      font-size: 2rem;
      font-weight: bold;
      text-align: left;
      margin-bottom: 32px;
      margin-left: 160px;
    }
    .form-group {
      margin-bottom: 22px;
      margin-left: 160px;
      max-width: 400px;
    }
    label {
      display: block;
      margin-bottom: 7px;
      font-weight: 600;
      color: #2D2C56;
    }
    input[type="text"],
    input[type="email"],
    input[type="file"] {
      width: 100%;
      padding: 12px;
      border-radius: 10px;
      border: 1px solid #ccc;
      font-size: 1rem;
      background: #fff;
      outline: none;
      transition: border 0.2s;
    }
    input[type="text"]:focus,
    input[type="email"]:focus {
      border: 1.5px solid #6559ff;
    }
    .profile-preview {
      display: block;
      margin: 0 auto 12px auto;
      border-radius: 50%;
      max-width: 100px;
      max-height: 100px;
      object-fit: cover;
      background: #fff;
      border: 2px solid #FFE149;
      box-shadow: 0 2px 8px 0 rgba(44,44,90,0.08);
    }
    button[type="submit"] {
      display: block;
      width: 100%;
      background: #2D2C56;
      color: white;
      border: none;
      padding: 14px;
      border-radius: 9999px;
      cursor: pointer;
      font-size: 1.1rem;
      font-weight: 600;
      margin-top: 18px;
      box-shadow: 0 2px 8px 0 rgba(44,44,90,0.08);
      transition: background 0.2s;
      margin-left: 160px;
      max-width: 400px;
    }
    button[type="submit"]:hover {
      background: #39387a;
    }
    @media (max-width: 900px) {
      .edit-profile-card {
        max-width: 98vw;
      }
      .edit-profile-header {
        height: 120px;
        border-top-left-radius: 24px;
        border-top-right-radius: 24px;
      }
      .edit-profile-avatar-wrap {
        left: 50%;
        transform: translateX(-50%);
        bottom: -64px;
      }
      .edit-profile-form-section {
        border-radius: 0 0 24px 24px;
        padding: 96px 16px 32px 16px;
      }
      .edit-profile-form-section h2,
      .form-group,
      button[type="submit"] {
        margin-left: 0;
        max-width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="edit-profile-card">
    <!-- Header -->
    <div class="edit-profile-header">
      <a href="/profile" class="edit-profile-back">&larr; Dashboard</a>
      <div class="edit-profile-avatar-wrap">
        <img src="/<%= user.photo %>" alt="Foto Profil" class="edit-profile-avatar-img">
      </div>
    </div>
    <!-- Form Section -->
    <div class="edit-profile-form-section">
      <h2>Edit Profil</h2>
      <form action="/profile/update" method="POST" enctype="multipart/form-data">
        <div class="form-group">
          <label for="username">Username</label>
          <input type="text" name="username" value="<%= user.username %>" required>
        </div>
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" name="email" value="<%= user.email %>" required>
        </div>
        <div class="form-group">
          <label for="photo">Foto Profil</label>
          <img src="/<%= user.photo %>" alt="Foto Profil" class="profile-preview">
          <input type="file" name="photo" accept="image/*">
        </div>
        <button type="submit">Simpan Perubahan</button>
      </form>
    </div>
  </div>
</body>
</html>
