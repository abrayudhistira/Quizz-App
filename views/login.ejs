<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Masuk ke Akun</title>

  <!-- Flaticon UIcons -->
  <link rel="stylesheet" href="https://cdn-uicons.flaticon.com/3.0.0/uicons-regular-rounded/css/uicons-regular-rounded.css" />

  <!-- Global CSS -->
  <link rel="stylesheet" href="/css/globals.css" />

  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100vh;
      background: var(--putih);
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      display: flex;
      width: 1400px;
      height: 760px;
      border-radius: 48px;
      overflow: hidden;
      position: relative;
      background: var(--kuning);
      box-shadow: 0 4px 40px rgba(0, 0, 0, 0.1);
    }

    .row-kiri,
    .row-kanan {
      flex: 1;
      height: 100%;
    }

    .row-kiri {
      background: var(--kuning);
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
      z-index: 1;
    }

    .row-kanan {
      background: var(--ungu);
      display: flex;
      justify-content: center;
      align-items: flex-end;
      border-radius: 48px;
      position: relative;
      z-index: 2;
    }

    .register-illustration {
      width: 95%;
      max-width: 500px;
      object-fit: contain;
      border-radius: 32px;
      display: block;
    }

    .left-box {
      width: 80%;
      max-width: 420px;
    }

    .judul {
      font-size: 3rem;
      font-weight: bold;
      color: var(--hitam);
      margin-bottom: 18px;
      text-align: center;
      letter-spacing: -1px;
    }

    .deskripsi {
      font-size: 1.25rem;
      color: var(--hitam);
      margin-bottom: 44px;
      text-align: center;
      line-height: 1.4;
    }

    .form-group {
      margin-bottom: 24px;
    }

    .input-wrapper {
      display: flex;
      align-items: center;
      width: 100%;
      background: var(--putih);
      border-radius: 32px;
      box-shadow: 0 1px 6px rgba(0, 0, 0, 0.05);
      height: 56px;
      padding: 0 20px;
    }

    .form-icon {
      font-size: 1.3rem;
      color: var(--abu);
      margin-right: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .form-input {
      flex: 1;
      height: 100%;
      border: none;
      outline: none;
      font-size: 1.15rem;
      background: transparent;
      color: var(--hitam);
    }

    .form-input::placeholder {
      color: var(--abu);
    }

    .btn-register {
      width: 100%;
      padding: 20px;
      border: none;
      border-radius: 32px;
      background: var(--navy);
      color: var(--putih);
      font-size: 1.3rem;
      font-weight: 600;
      cursor: pointer;
      margin-top: 8px;
      margin-bottom: 10px;
      letter-spacing: 1px;
      transition: background 0.2s;
      box-shadow: 0 1px 6px rgba(0, 0, 0, 0.05);
    }

    .btn-register:hover {
      background: #222060;
    }

    .link-text {
      text-align: center;
      margin-top: 14px;
      font-size: 1rem;
    }

    .message {
      text-align: center;
      margin-bottom: 16px;
      padding: 12px;
      border-radius: 8px;
      font-weight: 500;
    }

    .message.error {
      background: #ffe5e5;
      color: #a94442;
    }

    .message.success {
      background: #e5ffe5;
      color: #3c763d;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row-kiri">
      <div class="left-box">
        <div class="judul">Login</div>
        <div class="deskripsi">Masuk untuk melanjutkan<br />petualanganmu bersama kami!</div>
        <form id="loginForm" action="/login" method="POST">
          <div class="form-group">
            <div class="input-wrapper">
              <i class="fi fi-rr-user form-icon"></i>
              <input class="form-input" type="text" name="username" placeholder="Username" required />
            </div>
          </div>
          <div class="form-group">
            <div class="input-wrapper">
              <i class="fi fi-rr-eye-crossed form-icon"></i>
              <input class="form-input" type="password" name="password" placeholder="Kata Sandi" required />
            </div>
          </div>
          <button type="submit" class="btn-register">Masuk</button>
        </form>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="message error"><%= error %></div>
        <% } else if (typeof message !== 'undefined' && message) { %>
          <div class="message success"><%= message %></div>
        <% } %>

        <p class="link-text">Belum punya akun? <a href="/register">Daftar di sini</a></p>
      </div>
    </div>
    <div class="row-kanan">
      <img class="register-illustration" src="/images/login.png" alt="Login Illustration" />
    </div>
  </div>
</body>
</html>
